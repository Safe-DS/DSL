package safeds.ml.nn.converters

/**
 * The input conversion for a neural network defines the input parameters for the neural network.
 *
 * @param predictionName The name of the new column where the prediction will be stored.
 */
@Experimental
class InputConversionTable(
    @PythonName("prediction_name") predictionName: String = "prediction"
) sub InputConversion<TabularDataset, Table>
