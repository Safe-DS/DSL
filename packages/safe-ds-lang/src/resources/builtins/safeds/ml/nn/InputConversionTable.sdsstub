package safeds.ml.nn

/**
 * The input conversion for a neural network defines the input parameters for the neural network.
 *
 * @param featureNames The names of the features for the input table, used as features for the training.
 * @param targetName The name of the target for the input table, used as target for the training.
 */
@Experimental
class InputConversionTable() sub InputConversion<TabularDataset, Table>
