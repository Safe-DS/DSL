package tests.validation.other.declarations.parameters.constMustOnlyBeUsedForSpecificTypes
annotation MyAnnotation(
    // $TEST$ error "The parameter must be a constant but type 'Any' cannot be evaluated to a constant."
    anyParam: »Any«,
    // $TEST$ no error r"The parameter must be a constant but type '.*' cannot be evaluated to a constant\."
    intParam: »Int«,
)

@Pure
fun functionWithConstantParameters(
    // $TEST$ error "The parameter must be a constant but type 'Any' cannot be evaluated to a constant."
    const anyParam: »Any«,
    // $TEST$ no error r"The parameter must be a constant but type '.*' cannot be evaluated to a constant\."
    const intParam: »Int«,
)

@Pure
fun functionWithNormalParameters(
    // $TEST$ no error r"The parameter must be a constant but type '.*' cannot be evaluated to a constant\."
    anyParam: »Any«,
    // $TEST$ no error r"The parameter must be a constant but type '.*' cannot be evaluated to a constant\."
    intParam: »Int«,
)
