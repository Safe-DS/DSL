package tests.validation.other.types.callableTypes.mustNotHaveOptionalParameters

fun f(
    g: (
        // $TEST$ error "A callable type must not have optional parameters."
        p: Int = »1«,
    ) -> ()
)
