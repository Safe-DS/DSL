package tests.validation.types.checking.indexedAccessOnList

pipeline myPipeline {
    // $TEST$ no error r"Expected type 'Int' but got .*\."
    [1][»0«];

    // $TEST$ error "Expected type 'Int' but got 'literal<"">'."
    [0][»""«];

    // $TEST$ no error r"Expected type 'Int' but got .*\."
    {"": ""}[»""«];

    // $TEST$ no error r"Expected type 'Int' but got .*\."
    unresolved[»""«];
}
