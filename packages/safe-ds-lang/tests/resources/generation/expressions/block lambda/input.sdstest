package tests.generator.blockLambda

@Pure fun f1(param: (a: Int, b: Int) -> r: Int)
@Pure fun f2(param: () -> ())

@Pure fun g() -> a: Int

pipeline test {
    f1((a: Int, b: Int = 2) {
        yield d = g();
    });
    f1((a: Int, b: Int) {
        yield d = g();
    });
    f2(() {});
}
