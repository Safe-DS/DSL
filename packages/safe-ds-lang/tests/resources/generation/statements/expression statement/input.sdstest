package tests.generator.expressionStatement

@Pure fun f(param: () -> ())

@Pure fun g() -> result: Int

pipeline testPipeline {
    g();

    f(() {
        g();
    });
}

segment testSegment() {
    g();
}
