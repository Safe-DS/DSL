# Imports ----------------------------------------------------------------------

from tests.generator.maps import g1, g2, g3, h1, h2

# Pipelines --------------------------------------------------------------------

def test():
    g1({})
    g2({'a': 1.2, 'b': 1.0})
    g2({h2(): -0.5, 'b': h1()})
    g3({1.2: 'a', 1.0: 'b'})
    g3({5.6: 'c', h1(): h2()})
