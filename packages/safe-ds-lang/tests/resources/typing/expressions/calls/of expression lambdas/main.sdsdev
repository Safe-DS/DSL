package tests.typing.expressions.calls.ofExpressionLambdas

pipeline myPipeline {
    // $TEST$ serialization literal<1>
    »(() -> 1)()«;

    // $TEST$ serialization literal<1>
    »(() -> 1)?()«;
}
