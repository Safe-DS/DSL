package tests.typing.expressions.calls.ofAnnotations

annotation MyAnnotation

pipeline myPipeline {
    // $TEST$ serialization $unknown
    »MyAnnotation()«;

    // $TEST$ serialization $unknown
    »MyAnnotation?()«;
}
