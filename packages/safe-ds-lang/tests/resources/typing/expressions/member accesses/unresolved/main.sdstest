package tests.typing.expressions.memberAccesses.unresolved

class C

pipeline myPipeline {
    // $TEST$ serialization $unknown
    »C.unresolved«;

    // $TEST$ serialization $unknown
    »C?.unresolved«;
}
