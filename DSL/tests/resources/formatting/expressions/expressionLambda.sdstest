pipeline myPipeline {
    ()       ->

    1;
          (  a
         ,
          vararg    b   =
          1   )
     ->
            1;
}

// -----------------------------------------------------------------------------

pipeline myPipeline {
    () -> 1;
    (a, vararg b = 1) -> 1;
}
