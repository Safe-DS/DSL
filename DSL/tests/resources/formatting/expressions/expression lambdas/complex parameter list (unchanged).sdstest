pipeline p {
    (
        a: A.B,
        b: Int
    ) -> 1;
}

// -----------------------------------------------------------------------------

pipeline p {
    (
        a: A.B,
        b: Int
    ) -> 1;
}
