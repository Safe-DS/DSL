pipeline p {
    ( a: Int ,  b: Int ,  c: Int ,  d: Int ) -> 1;
}

// -----------------------------------------------------------------------------

pipeline p {
    (
        a: Int,
        b: Int,
        c: Int,
        d: Int
    ) -> 1;
}
