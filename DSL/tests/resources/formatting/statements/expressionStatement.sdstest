pipeline myPipeline {
    call()
    ;
    () {
               call()    ;
    };
}

// -----------------------------------------------------------------------------

pipeline myPipeline {
    call();
    () {
        call();
    };
}
