pipeline p {
    (@Annotation a: Int ,  b: Int , ) {};
}

// -----------------------------------------------------------------------------

pipeline p {
    (
        @Annotation
        a: Int,
        b: Int,
    ) {};
}
