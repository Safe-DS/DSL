segment mySegment(
    x: Int < * ,  in  Number ,  out  Number ,  T  =  Number > ?
) {}

// -----------------------------------------------------------------------------

segment mySegment(
    x: Int<*, in Number, out Number, T = Number>?
) {}
