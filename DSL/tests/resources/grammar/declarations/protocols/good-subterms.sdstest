// $TEST$ no_syntax_error

class Table {
    protocol {
        subterm term1 = f;
        subterm term2 = g;

        term1 | term2
    }
}

fun loadDataset() -> table: Table

pipeline test {
    val t = loadDataset();
}
