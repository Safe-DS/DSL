package tests.validation.declarations.pipelines.noYield

step s() {
	// no_semantic_error "Yield must not be used in a pipeline."
    »yield a« = 1;
}

pipeline p {
	// semantic_error "Yield must not be used in a pipeline."
    »yield a« = 1;

    () {
        // no_semantic_error "Yield must not be used in a pipeline."
        »yield a« = 1;
    };
}
