class C sub ( Int  ) {
    constraint
     { T
     sub (
         Int  )  }
    attr a: (  Int   )
}

step s(p1: ( Int
), p2: union<(  Int )>) -> r: (
 Int ) {}

// -----------------------------------------------------------------------------

class C sub (Int) {
    constraint {
        T sub (Int)
    }

    attr a: (Int)
}

step s(p1: (Int), p2: union<(Int)>) -> r: (Int) {}
