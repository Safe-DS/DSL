[//]: # (DO NOT EDIT THIS FILE DIRECTLY. Instead, edit the corresponding stub file and execute `npm run docs:api`.)

# <code class="doc-symbol doc-symbol-class"></code> `ColumnType` {#safeds.data.tabular.typing.ColumnType data-toc-label='[class] ColumnType'}

The type of a column in a table.

Use the static factory methods to create instances of this class.

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="8"
    class ColumnType {
        /**
         * Whether the column type is a floating point type.
         */
        @PythonName("is_float") attr isFloat: Boolean
        /**
         * Whether the column type is an integer type (signed or unsigned).
         */
        @PythonName("is_int") attr isInt: Boolean
        /**
         * Whether the column type is a numeric type.
         */
        @PythonName("is_numeric") attr isNumeric: Boolean
        /**
         * Whether the column type is a signed integer type.
         */
        @PythonName("is_signed_int") attr isSignedInt: Boolean
        /**
         * Whether the column type is a temporal type.
         */
        @PythonName("is_temporal") attr isTemporal: Boolean
        /**
         * Whether the column type is an unsigned integer type.
         */
        @PythonName("is_unsigned_int") attr isUnsignedInt: Boolean

        /**
         * Create a `float32` column type (32-bit floating point number).
         */
        @Pure
        static fun float32() -> result1: ColumnType

        /**
         * Create a `float64` column type (64-bit floating point number).
         */
        @Pure
        static fun float64() -> result1: ColumnType

        /**
         * Create an `int8` column type (8-bit signed integer).
         */
        @Pure
        static fun int8() -> result1: ColumnType

        /**
         * Create an `int16` column type (16-bit signed integer).
         */
        @Pure
        static fun int16() -> result1: ColumnType

        /**
         * Create an `int32` column type (32-bit signed integer).
         */
        @Pure
        static fun int32() -> result1: ColumnType

        /**
         * Create an `int64` column type (64-bit signed integer).
         */
        @Pure
        static fun int64() -> result1: ColumnType

        /**
         * Create a `uint8` column type (8-bit unsigned integer).
         */
        @Pure
        static fun uint8() -> result1: ColumnType

        /**
         * Create a `uint16` column type (16-bit unsigned integer).
         */
        @Pure
        static fun uint16() -> result1: ColumnType

        /**
         * Create a `uint32` column type (32-bit unsigned integer).
         */
        @Pure
        static fun uint32() -> result1: ColumnType

        /**
         * Create a `uint64` column type (64-bit unsigned integer).
         */
        @Pure
        static fun uint64() -> result1: ColumnType

        /**
         * Create a `date` column type.
         */
        @Pure
        static fun date() -> result1: ColumnType

        /**
         * Create a `datetime` column type.
         *
         * @param timeZone The time zone. If None, values are assumed to be in local time. This is different from setting the time zone
         * to `"UTC"`. Any TZ identifier defined in the
         * [tz database](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones) is valid.
         */
        @Pure
        static fun datetime(
            @PythonName("time_zone") timeZone: String? = null
        ) -> result1: ColumnType

        /**
         * Create a `duration` column type.
         */
        @Pure
        static fun duration() -> result1: ColumnType

        /**
         * Create a `time` column type.
         */
        @Pure
        static fun time() -> result1: ColumnType

        /**
         * Create a `string` column type.
         */
        @Pure
        static fun string() -> result1: ColumnType

        /**
         * Create a `binary` column type.
         */
        @Pure
        static fun binary() -> result1: ColumnType

        /**
         * Create a `boolean` column type.
         */
        @Pure
        static fun boolean() -> result1: ColumnType

        /**
         * Create a `null` column type.
         */
        @Pure
        static fun ^null() -> result1: ColumnType
    }
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-attribute"></code> `isFloat` {#safeds.data.tabular.typing.ColumnType.isFloat data-toc-label='[attribute] isFloat'}

Whether the column type is a floating point type.

**Type:** [`Boolean`][safeds.lang.Boolean]

## <code class="doc-symbol doc-symbol-attribute"></code> `isInt` {#safeds.data.tabular.typing.ColumnType.isInt data-toc-label='[attribute] isInt'}

Whether the column type is an integer type (signed or unsigned).

**Type:** [`Boolean`][safeds.lang.Boolean]

## <code class="doc-symbol doc-symbol-attribute"></code> `isNumeric` {#safeds.data.tabular.typing.ColumnType.isNumeric data-toc-label='[attribute] isNumeric'}

Whether the column type is a numeric type.

**Type:** [`Boolean`][safeds.lang.Boolean]

## <code class="doc-symbol doc-symbol-attribute"></code> `isSignedInt` {#safeds.data.tabular.typing.ColumnType.isSignedInt data-toc-label='[attribute] isSignedInt'}

Whether the column type is a signed integer type.

**Type:** [`Boolean`][safeds.lang.Boolean]

## <code class="doc-symbol doc-symbol-attribute"></code> `isTemporal` {#safeds.data.tabular.typing.ColumnType.isTemporal data-toc-label='[attribute] isTemporal'}

Whether the column type is a temporal type.

**Type:** [`Boolean`][safeds.lang.Boolean]

## <code class="doc-symbol doc-symbol-attribute"></code> `isUnsignedInt` {#safeds.data.tabular.typing.ColumnType.isUnsignedInt data-toc-label='[attribute] isUnsignedInt'}

Whether the column type is an unsigned integer type.

**Type:** [`Boolean`][safeds.lang.Boolean]

## <code class="doc-symbol doc-symbol-static-function"></code> `binary` {#safeds.data.tabular.typing.ColumnType.binary data-toc-label='[static-function] binary'}

Create a `binary` column type.

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="133"
    @Pure
    static fun binary() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `boolean` {#safeds.data.tabular.typing.ColumnType.boolean data-toc-label='[static-function] boolean'}

Create a `boolean` column type.

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="139"
    @Pure
    static fun boolean() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `date` {#safeds.data.tabular.typing.ColumnType.date data-toc-label='[static-function] date'}

Create a `date` column type.

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="97"
    @Pure
    static fun date() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `datetime` {#safeds.data.tabular.typing.ColumnType.datetime data-toc-label='[static-function] datetime'}

Create a `datetime` column type.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `timeZone` | [`String?`][safeds.lang.String] | The time zone. If None, values are assumed to be in local time. This is different from setting the time zone to `"UTC"`. Any TZ identifier defined in the [tz database](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones) is valid. | `#!sds null` |

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="107"
    @Pure
    static fun datetime(
        @PythonName("time_zone") timeZone: String? = null
    ) -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `duration` {#safeds.data.tabular.typing.ColumnType.duration data-toc-label='[static-function] duration'}

Create a `duration` column type.

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="115"
    @Pure
    static fun duration() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `float32` {#safeds.data.tabular.typing.ColumnType.float32 data-toc-label='[static-function] float32'}

Create a `float32` column type (32-bit floating point number).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="37"
    @Pure
    static fun float32() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `float64` {#safeds.data.tabular.typing.ColumnType.float64 data-toc-label='[static-function] float64'}

Create a `float64` column type (64-bit floating point number).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="43"
    @Pure
    static fun float64() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `int16` {#safeds.data.tabular.typing.ColumnType.int16 data-toc-label='[static-function] int16'}

Create an `int16` column type (16-bit signed integer).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="55"
    @Pure
    static fun int16() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `int32` {#safeds.data.tabular.typing.ColumnType.int32 data-toc-label='[static-function] int32'}

Create an `int32` column type (32-bit signed integer).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="61"
    @Pure
    static fun int32() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `int64` {#safeds.data.tabular.typing.ColumnType.int64 data-toc-label='[static-function] int64'}

Create an `int64` column type (64-bit signed integer).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="67"
    @Pure
    static fun int64() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `int8` {#safeds.data.tabular.typing.ColumnType.int8 data-toc-label='[static-function] int8'}

Create an `int8` column type (8-bit signed integer).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="49"
    @Pure
    static fun int8() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `null` {#safeds.data.tabular.typing.ColumnType.null data-toc-label='[static-function] null'}

Create a `null` column type.

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="145"
    @Pure
    static fun ^null() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `string` {#safeds.data.tabular.typing.ColumnType.string data-toc-label='[static-function] string'}

Create a `string` column type.

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="127"
    @Pure
    static fun string() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `time` {#safeds.data.tabular.typing.ColumnType.time data-toc-label='[static-function] time'}

Create a `time` column type.

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="121"
    @Pure
    static fun time() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `uint16` {#safeds.data.tabular.typing.ColumnType.uint16 data-toc-label='[static-function] uint16'}

Create a `uint16` column type (16-bit unsigned integer).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="79"
    @Pure
    static fun uint16() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `uint32` {#safeds.data.tabular.typing.ColumnType.uint32 data-toc-label='[static-function] uint32'}

Create a `uint32` column type (32-bit unsigned integer).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="85"
    @Pure
    static fun uint32() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `uint64` {#safeds.data.tabular.typing.ColumnType.uint64 data-toc-label='[static-function] uint64'}

Create a `uint64` column type (64-bit unsigned integer).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="91"
    @Pure
    static fun uint64() -> result1: ColumnType
    ```
    { data-search-exclude }

## <code class="doc-symbol doc-symbol-static-function"></code> `uint8` {#safeds.data.tabular.typing.ColumnType.uint8 data-toc-label='[static-function] uint8'}

Create a `uint8` column type (8-bit unsigned integer).

**Results:**

| Name | Type | Description |
|------|------|-------------|
| `result1` | [`ColumnType`][safeds.data.tabular.typing.ColumnType] | - |

??? quote "Stub code in `ColumnType.sdsstub`"

    ```sds linenums="73"
    @Pure
    static fun uint8() -> result1: ColumnType
    ```
    { data-search-exclude }
