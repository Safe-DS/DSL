codecov:
  notify:
    after_n_builds: 3

comment:
  layout: 'header, diff, flags, components, files'

component_management:
  individual_components:
    - component_id: module_dsl
      name: dsl
      paths:
        - DSL/**
    - component_id: module_runner
      name: runner
      paths:
        - Runner/**
    - component_id: module_dsl_langium
      name: dsl-langium
      paths:
        - DSL-langium/**
