package test.scoping.annotationCalls.onResult

// $TEST$ target before 
annotation »Before«

fun myFunction() -> (
    // $TEST$ references before
    @»Before«
    // $TEST$ references after
    @»After«
    // $TEST$ unresolved
    @»Unresolved«
    myResult: Int
)

// $TEST$ target after
annotation »After«
