package tests.scoping.references.direct.toBlockLambdaResults.ofContainingBlockLambda

pipeline myPipeline {
    () {
        yield lambdaResult = 1;
        // $TEST$ unresolved
        »lambdaResult«;
    };
}
