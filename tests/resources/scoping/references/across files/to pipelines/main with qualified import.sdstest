package tests.scoping.references.acrossFiles.toPipelines

import safeds.scoping.references.acrossFiles.toPipelines.myPipeline
import tests.scoping.references.acrossFiles.toPipelines.other.pipelineInAnotherPackage
import pipelineWithoutPackage

segment mySegment() {
    // $TEST$ references safeds_myPipeline
    »myPipeline«;

    // $TEST$ references same_pipelineInSamePackage
    »pipelineInSamePackage«;

    // $TEST$ references safeds_pipelineInSafeDsPackage
    »pipelineInSafeDsPackage«;

    // $TEST$ references other_pipelineInAnotherPackage
    »pipelineInAnotherPackage«;

    // $TEST$ unresolved
    »pipelineWithoutPackage«;
}
