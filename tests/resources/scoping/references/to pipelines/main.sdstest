package tests.scoping.references.toPipelines

// $TEST$ target before
pipeline »before« {}

segment mySegment() {
    // $TEST$ references before
    »before«;

    // $TEST$ references after
    »after«;
}

// $TEST$ target after
pipeline »after« {}
