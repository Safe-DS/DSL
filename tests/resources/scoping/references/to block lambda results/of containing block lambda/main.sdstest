package tests.scoping.references.toBlockLambdaResults.ofContainingBlockLambda

pipeline myPipeline {
    () {
        yield lambdaResult = 1;
        // $TEST$ unresolved
        »lambdaResult«;
    };
}
