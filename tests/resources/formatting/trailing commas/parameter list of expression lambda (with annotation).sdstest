pipeline p {
    (@Annotation a: Int ,  b: Int , ) -> 1;
}

// -----------------------------------------------------------------------------

pipeline p {
    (
        @Annotation
        a: Int,
        b: Int,
    ) -> 1;
}
