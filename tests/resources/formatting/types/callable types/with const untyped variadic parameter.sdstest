segment mySegment(
    f: ( const  vararg  a )  ->  ( ),
    g: ( vararg  const  a )  ->  ( )
) {}

// -----------------------------------------------------------------------------

segment mySegment(
    f: (const vararg a) -> (),
    g: (vararg const a) -> ()
) {}
