pipeline myPipeline {
    ( const  vararg  a )  ->  1;
    ( vararg  const  a )  ->  1;
}

// -----------------------------------------------------------------------------

pipeline myPipeline {
    (const vararg a) -> 1;
    (vararg const a) -> 1;
}
