pipeline myPipeline {
    ( const  vararg  a )  { };
    ( vararg  const  a )  { };
}

// -----------------------------------------------------------------------------

pipeline myPipeline {
    (const vararg a) {};
    (vararg const a) {};
}
