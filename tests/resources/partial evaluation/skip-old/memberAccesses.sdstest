package tests.partialEvaluation.memberAccesses

pipeline successfulResultAccess {
    val lambda = () {
        yield result = 1;
    };

    lambda().result;
}

pipeline failedResultAccess {
    val lambda = () {
        yield result = 1;
    };

    lambda().result1;
}
