package tests.partialValidation.recursiveCases.prefixOperations

pipeline `not` {
    // $TEST$ serialization true
    »not false«;

    // $TEST$ serialization false
    »not true«;

    // $TEST$ serialization $unknown
    »not 0«;

    // $TEST$ serialization $unknown
    »not unresolved«;
}

pipeline minus {
    // $TEST$ serialization -1.5
    »-1.5«;

    // $TEST$ serialization -1
    »-1«;

    // $TEST$ serialization $unknown
    »-true«;

    // $TEST$ serialization $unknown
    »-unresolved«;
}
