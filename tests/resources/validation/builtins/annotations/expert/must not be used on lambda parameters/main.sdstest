package tests.validation.builtins.expert.mustNotBeUsedOnLambdaParameters

pipeline myPipeline {

    // $TEST$ error "The '@Expert' annotation is not applicable to lambda parameters."
    // $TEST$ error "The '@Expert' annotation is not applicable to lambda parameters."
    val f = (»@Expert« a: Int, »@Expert« b: Int = 3) {};

    // $TEST$ error "The '@Expert' annotation is not applicable to lambda parameters."
    // $TEST$ error "The '@Expert' annotation is not applicable to lambda parameters."
    val g = (»@Expert« a: Int, »@Expert« b: Int = 3) -> 1;
}
