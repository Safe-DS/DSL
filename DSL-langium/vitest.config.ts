import { defineConfig } from 'vitest/config';

export default defineConfig({
    test: {
        // TODO: enable when vitest 0.30.0 is released
        // chaiConfig: {
        //     truncateThreshold: 0,
        // },
    },
});
